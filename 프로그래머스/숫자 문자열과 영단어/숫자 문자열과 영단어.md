해당 문제는 앞에서 배운 것처럼 정규식을 사용하여 풀이를 하려고 했으나

replaceAll 함수를 사용한다면 정규식을 사용하지 않고도 간단하게 풀이할 수 있었습니다.

replaceAll 함수의 경우, 대체하는 대상의 문자열과 대체할 값을 파라미터로 지정해주기만

한다면, 문자열에서 해당 값을 찾아서 알아서 대체해주기 때문입니다.

이를 위해서 0 ~ 10까지의 영단어를 array 배열에 삽입하였고,

for 반복문을 통해서 0 ~ 10까지 i가 반복되면서, 이에 해당하는 영단어를 그에 맞는

숫자로 대체하고, 이는 아직 String 자료형이기 때문에 반복문이 종료되고,

s에 있는 모든 영단어가 숫자로 바뀌게 된다면 이를 parseInt를 통해서 숫자로 변환하여

반환하게 됩니다.

위처럼 작성하고, 코드를 실행한 결과, 4개의 테스트를 모두 통과하였습니다.
