해당 문제는 주어지는 정수배열과, K 라는 이름을 가진

변수에 입력된 값을 사용하여

배열 내 K 보다 작은 수를 2를 곱하고 더하는 과정을 통해 새로운

수를 만들고, 이를 다시 배열에 집어넣어

모든 수를 K 보다 크게 만든 후,

해당 과정에서 소요된 계산의 수를 출력하는 문제로 보였습니다.

이를 해결하기 위해서 최소힙을 사용하여 문제를 풀이하였고

먼저 힙에 scoville 배열의 원소를 집어넣기 위해서 add 메소드를

for 반복문을 scoville 배열의 length 만큼 반복하도록

작성하였습니다.

그 후, while 조건문 내에 peek 메소드를 사용해서 힙의 첫번째 값,

즉 제일 작은 값이 K 이하일 경우에 반복문이 동작하도록 하였습니다.

또한 힙의 size가 최소 2 이상이어야 해당 계산을 통해 더 큰수를

만들 수 있기에 size가 2 이상이 아니라면 바로 -1을 반환하도록

작성하였고, 그 후 poll 메소드를 2번 사용해서 힙에서 가장 작은

2개의 수를 f, s 라는 변수에 각각 저장한 후

이를 f + (s\*2) 로 계산하여 add 메소드를 통해 다시 힙에 집어넣는

과정을 거치도록 작성하였습니다.

해당 과정은 힙의 가장 최솟값이 K 변수에 입력된 값보다 클때까지

반복됩니다.

해당 계산이 몇번 이루어지는 지를 파악하기 위해서

answer 변수를 조건문 밖에서 초기화 한 후, ++ 를 사용하였고

while 조건문이 종료되고 난 후, answer 값을 반환하면서 코드의

동작이 종료되게 됩니다.

해당 코드 실행 결과는 성공이었습니다.
