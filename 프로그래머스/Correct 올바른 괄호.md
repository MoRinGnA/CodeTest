해당 문제를 보자마자 Deque 인터페이스를 통해서

풀어야 할 문제라는 생각이 들었습니다.

또한 s라는 문자열을 입력받아서, 검사 과정을 거쳐

해당 괄호묶음이 TRUE 인지 FALSE 인지를 확인해야 하는데

이를 charAt 과 반복문을 통해서 받아오고,

if else 문과 push 및 pop 메소드를 통해서 데이터를

밀어넣거나 삭제하는 기능을 구현하면 성공적으로 구현할 수

있을 것이라는 생각이 들어 구현해보았습니다.

그것이 첫번째 풀이라는 커밋입니다.

하지만 프로그래머스 사이트에서 코드를 실행해보니

4개 중 2개만 성공이라는 문구가 뜨고 오류코드가 나와

이를 분석해보았더니, 비어있는 스택에서 pop 메소드가

실행되기 때문에 오류가 발생한다는 것을 깨닫게 되었습니다.

해당 문제를 수정하기 위해서는 If else 문을 수정할면 될 것이라는

생각이 들었고, else if 문을 삭제하고, else로 통합하여,

pop 메소드를 실행하기 전에 if 조건문을 하나 더 삽입하여

isEmpty를 사용해 스택이 비어있는지를 검사하고

비어있다면 answer 변수에 false를 반환하고

비어있지 않다면 pop 메소드를 실행하는 코드로 수정하였습니다.

그렇게 수정하고, 코드를 실행한 결과는 3개의 테스트는 통과했지만,

마지막 테스트는 통과하지 못했다는 결과를 도출했습니다.

해당 문제는 반복문이 끝나고 난 후, 열린 괄호가 그대로

스택에 남아있는데 반복문이 종료되니, 그냥 true가 반환이

되어버려 발생하는 문제라는 것을 알았고,

이를 보완하기 위해서 코드 마지막에 isEmpty를 삽입하여

마지막에 스택에 무언가 없다면 true, 있다면 false를 반환하는

코드를 작성하는 것으로 최종 수정을 마치게 되었습니다.
